<div class="columns">

  <div class="is-active m-auto p-3">
    <div class="m-auto">
      <%= form_with(url: search_shoes_path, method: :get, local: true) do |f| %>
        <div class="field has-addons has-addons-right">
          <p class="control">
          </p>
        </div>
      <% end %>
    </div>
  </div>
</div>