<div class="container">
  <h1 class="title is-1">Orders</h1>
  <table class="table is-fullwidth">
    <thead>
      <tr>
        <th>ID</th>
        <th>Status</th>
        <th>Subtotal</th>
        <th>Email</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% @orders.each do |order| %>
        <tr>
          <th><%= order.id %></th>
          <td class="has-text-danger is-uppercase">

          </td>
          <td><%= number_to_currency(order.subtotal) %></td>
          <td><%= order.user.email %></td>
          <td>
            <%= link_to 'Detail', order, class: 'button is-link is-small mr-2' %>
            <%= link_to 'Delete', order_path(order.id), 'data-turbo-method': :delete, class: 'button is-danger is-email' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
